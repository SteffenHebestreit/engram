# torch (CPU) is installed separately in bench/Dockerfile to avoid the CUDA wheel
sentence-transformers>=3.0
rank-bm25>=0.2.2
datasets>=3.0          # HotpotQA loading for the multi-hop benchmark
FlagEmbedding>=1.3     # BGE-M3 dense+sparse+colbert outputs for the hybrid probe
scipy>=1.11            # sparse CSR matmul for the learned-sparse channel probe
